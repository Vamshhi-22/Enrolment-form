import { InformfilterPipe } from './informfilter.pipe';

describe('InformfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InformfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
